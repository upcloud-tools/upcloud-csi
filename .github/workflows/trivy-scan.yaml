name: Trivy Scan

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

on:
  push:
    branches:
      - main
    paths:
      - "**/*.go"
      - go.mod
      - go.sum
      - build-image.sh
      - '.github/workflows/trivy-scan.yaml'
      - '.trivyignore.yaml'
  pull_request:
    branches:
      - main
    paths:
      - "**/*.go"
      - go.mod
      - go.sum
      - build-image.sh
      - '.github/workflows/trivy-scan.yaml'
      - '.trivyignore.yaml'
  schedule:
    - cron: '5 5 * * 2'

permissions:
  contents: read

jobs:
  analyze:
    permissions:
      contents: read
      actions: read
      security-events: write
      packages: read
    timeout-minutes: 10
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - if: github.event_name == 'schedule' || github.event_name == 'push'
        name: Log in to GHCR
        uses: ./.github/actions/ghcr-login

      - name: Run Trivy vulnerability scanner (filesystem)
        uses: aquasecurity/trivy-action@ed142fd0673e97e23eac54620cfb913e5ce36c25 # v0.36.0
        with:
          scan-type: 'fs'
          scan-ref: .
          format: 'sarif'
          output: 'trivy-fs-results.sarif'
          severity: 'CRITICAL,HIGH'
          ignore-unfixed: true
          trivyignores: '.trivyignore.yaml'

      - name: Upload filesystem scan results to GitHub Security tab
        uses: github/codeql-action/upload-sarif@ff2f1c621b7f889edc0d3c761ac2e6a3f8cdb0dd # v4.37.7
        with:
          sarif_file: 'trivy-fs-results.sarif'
          category: trivy-fs

      - if: github.event_name == 'pull_request'
        uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version-file: go.mod

      - if: github.event_name == 'pull_request'
        name: Build image for PR scan
        run: make container-build CONTAINER_REPO=upcloud-csi IMAGE_TAG=latest

      - if: github.event_name == 'pull_request'
        name: Export image to tar archive
        run: podman save -o image.tar upcloud-csi:latest

      - if: github.event_name == 'pull_request'
        name: Run Trivy vulnerability scanner (PR image)
        uses: aquasecurity/trivy-action@ed142fd0673e97e23eac54620cfb913e5ce36c25 # v0.36.0
        with:
          input: image.tar
          format: 'sarif'
          output: 'trivy-pr-image-results.sarif'
          severity: 'CRITICAL,HIGH'
          ignore-unfixed: true

      - if: github.event_name == 'pull_request'
        name: Upload PR image scan results to GitHub Security tab
        uses: github/codeql-action/upload-sarif@ff2f1c621b7f889edc0d3c761ac2e6a3f8cdb0dd # v4.37.7
        with:
          sarif_file: 'trivy-pr-image-results.sarif'
          category: trivy-image

      - if: github.event_name == 'push'
        id: scan-pushed-image
        name: Run Trivy vulnerability scanner (pushed image)
        continue-on-error: true
        uses: aquasecurity/trivy-action@ed142fd0673e97e23eac54620cfb913e5ce36c25 # v0.36.0
        with:
          image-ref: 'ghcr.io/${{ github.repository }}:${{ github.sha }}'
          format: 'sarif'
          output: 'trivy-image-results.sarif'
          severity: 'CRITICAL,HIGH'
          ignore-unfixed: true

      - if: github.event_name == 'push' && steps.scan-pushed-image.outcome == 'success'
        name: Upload pushed image scan results to GitHub Security tab
        uses: github/codeql-action/upload-sarif@ff2f1c621b7f889edc0d3c761ac2e6a3f8cdb0dd # v4.37.7
        with:
          sarif_file: 'trivy-image-results.sarif'
          category: trivy-image

      - if: github.event_name == 'schedule'
        id: scan-scheduled-image
        name: Run Trivy vulnerability scanner (image)
        continue-on-error: true
        uses: aquasecurity/trivy-action@ed142fd0673e97e23eac54620cfb913e5ce36c25 # v0.36.0
        with:
          image-ref: 'ghcr.io/${{ github.repository }}:latest'
          format: 'sarif'
          output: 'trivy-image-results.sarif'
          severity: 'CRITICAL,HIGH'
          ignore-unfixed: true

      - if: github.event_name == 'schedule' && steps.scan-scheduled-image.outcome == 'success'
        name: Upload image scan results to GitHub Security tab
        uses: github/codeql-action/upload-sarif@ff2f1c621b7f889edc0d3c761ac2e6a3f8cdb0dd # v4.37.7
        with:
          sarif_file: 'trivy-image-results.sarif'
          category: trivy-image
