apiVersion: v2
name: upcloud-csi
description: CSI driver for UpCloud block storage
type: application
version: "1.2.0"
appVersion: "v2.5.1"
kubeVersion: ">=1.21.0"
keywords:
  - csi
  - storage
  - upcloud
  - kubernetes
home: https://github.com/upcloud-tools/upcloud-csi
sources:
  - https://github.com/upcloud-tools/upcloud-csi
maintainers:
  - name: Leonardo (aardbol)
    url: https://github.com/aardbol
annotations:
  artifacthub.io/maintainers: |
    - name: aardbol
  artifacthub.io/category: storage
  artifacthub.io/license: MIT
  artifacthub.io/links: |
    - name: Support
      url: https://github.com/upcloud-tools/upcloud-csi/issues
  artifacthub.io/changes: |
    - "Disable StorageClasses by default"
    - "Add VolumeSnapshot CRD conflict resolution instructions to README"
    - "Add --skip-crds install option for clusters with pre-existing CRDs"
    - "Consolidate install/upgrade into helm upgrade --install"
    - "Add upgrade instructions to README"
    - "Add shell history protection note to install commands"
