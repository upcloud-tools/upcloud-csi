apiVersion: storage.k8s.io/v1
kind: CSIDriver
metadata:
  name: {{ .Values.controller.csiDriver.name }}
  labels:
    {{- include "upcloud-csi.labels" . | nindent 4 }}
spec:
  attachRequired: {{ .Values.controller.csiDriver.attachRequired }}
  podInfoOnMount: {{ .Values.controller.csiDriver.podInfoOnMount }}
  fsGroupPolicy: {{ .Values.controller.csiDriver.fsGroupPolicy }}
